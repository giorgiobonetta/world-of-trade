{
  "name": "world-of-trade-mobile",
  "private": true,
  "version": "0.7.3-public-auth",
  "description": "World of Trade \u2014 public landing with dedicated sign-in and registration pages",
  "scripts": {
    "cap:sync": "npx cap sync",
    "android:add": "npx cap add android",
    "android:open": "npx cap open android",
    "android:run": "npx cap run android",
    "ios:add": "npx cap add ios",
    "ios:open": "npx cap open ios",
    "ios:run": "npx cap run ios",
    "assets": "npx capacitor-assets generate"
  },
  "dependencies": {
    "@capacitor/core": "*",
    "@capacitor/app": "*",
    "@capacitor/haptics": "*",
    "@capacitor/status-bar": "*",
    "@capacitor/splash-screen": "*"
  },
  "devDependencies": {
    "@capacitor/cli": "*",
    "@capacitor/android": "*",
    "@capacitor/ios": "*",
    "@capacitor/assets": "*"
  }
}
